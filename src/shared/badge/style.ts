import { StyleSheet } from 'react-native';

export const badgeStyles = StyleSheet.create({
  container: {
    position: 'relative',
  },
  dot: {
    position: 'absolute',
    top: -1,
    right: -2,
  },
});
