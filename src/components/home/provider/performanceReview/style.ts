import { StyleSheet } from 'react-native';
import COLORS from 'utils/colors';

export const performanceReviewStyles = StyleSheet.create({
  headerBar: {
    backgroundColor: COLORS.twilightBlueTwo,
    shadowOpacity: 0,
  },
});
