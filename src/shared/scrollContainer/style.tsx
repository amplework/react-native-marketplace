import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  keyboardStyle: { height: '100%', width: '100%' },
  extraBottom: { paddingBottom: 200 },
  scrollStyle: { flex: 1, width: '100%' },
});

export default styles;
