import { StyleSheet } from 'react-native';
import COLORS from 'utils/colors';

export const clientSalesStyles = StyleSheet.create({
  container: {
    backgroundColor: COLORS.whiteFour,
    paddingTop: 32,
    width: '100%',
    height: '100%',
    justifyContent: 'center',
  },
});
